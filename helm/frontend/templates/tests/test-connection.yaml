apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mlops-st.fullname" . }}-test-connection"
  labels:
    {{- include "mlops-st.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mlops-st.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
