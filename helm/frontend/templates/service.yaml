apiVersion: v1
kind: Service
metadata:
  name: {{ include "mlops-st.fullname" . }}
  labels:
    {{- include "mlops-st.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mlops-st.selectorLabels" . | nindent 4 }}
